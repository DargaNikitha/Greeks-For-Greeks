class Solution:
    def nPr(self, n: int, r: int) -> int:
        # code here
        def factorial(n):
            fact=1
            for i in range(1,n+1):
                fact*=i
            return fact
        a=factorial(n)
        b=factorial(n-r)
        return int(a/b)
