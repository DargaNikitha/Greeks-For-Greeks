class Solution:
    def nCr(self, n, r):
        # code here
        def factorial(n):
            fact=1
            for i in range(1,n+1):
                fact*=i
            return fact
        a=factorial(n)
        b=factorial(r)
        c=factorial(n-r)
        return int(a/(b*c))
