class Solution:
    def getMinMax(self, arr):
        # code here
        min=float('inf')
        max=float('-inf')
        for i in range(len(arr)):
            if(arr[i]>max):
                max=arr[i]
            if(arr[i]<min):
                min=arr[i]
        return (min,max)
